gui:
  theme:
    activeBorderColor:
      - green
      - bold
  showIcons: true

git:
  paging:
    colorArg: always
    pager: delta --dark --paging=never
    useConfig: true
  commit:
    signOff: false

keybinding:
  universal:
    quit: 'q'
    return: '<esc>'
